# Notes

A second, present companion file in the same minimal doc set as
01-REQUIREMENTS.md. Not part of `required-files.files`, so it has no
bearing on this fixture's missing-file finding (02-ARCHITECTURE.md is the
file deliberately absent in the fail/ variant and restored here).
