# Notes

Same minimal doc set as fail/builtin-disabled/, with the configured custom
pattern occurrence removed -- the one defect corrected, otherwise identical.
The built-in `</content>` occurrence remains present but is correctly not
flagged since builtin_patterns is still false.
